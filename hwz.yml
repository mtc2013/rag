---
scenarios:
  - &test
    match: "Test"

features:
  - &step1-1
    FEATURE: features/test.feature
    pass: true
    if_pass:
      - &step1-2
        FEATURE: features/test.feature
        version: 2
        desc: "results false"
        failures:
          - *test